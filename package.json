{
  "name": "focuslock-sync",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "theme:generate": "node scripts/generate-theme.mjs",
    "theme:check": "node scripts/generate-theme.mjs --check",
    "convex:dev": "convex dev",
    "convex:deploy": "convex deploy"
  },
  "dependencies": {
    "convex": "^1.45.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
